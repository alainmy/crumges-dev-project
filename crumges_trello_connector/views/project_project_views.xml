<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="edit_project_trello_inherit" model="ir.ui.view">
        <field name="name">project.project.form.trello.inherit</field>
        <field name="model">project.project</field>
        <field name="inherit_id" ref="project.edit_project"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="action_archive_trello_board" type="object" string="Archivar Tablero Trello" class="oe_highlight" invisible="stage_fold == False or trello_board_reference == False"/>
                <field name="stage_fold" invisible="1"/>
            </xpath>
            <xpath expr="//notebook" position="inside">
                <page string="Trello" name="trello_sync_page">
                    <group>
                        <group>
                            <field name="trello_sync" widget="boolean_toggle"/>
                            <field name="trello_board_id" 
                                   invisible="trello_sync == False" 
                                   readonly="trello_board_reference != False" />
                            <field name="trello_board_reference" readonly="1" invisible="1" />
                            <field name="trello_board_url" 
                                   widget="url" 
                                   readonly="1" 
                                   invisible="trello_board_url == False or trello_sync == False"/>
                        </group>
                        <group string="Enviar a Trello (Sincronización Manual)" invisible="trello_sync == False or trello_board_reference == False">
                            <button name="action_debug_sync_stages" type="object" string="Enviar Etapas a Trello" class="btn-secondary"/>
                            <button name="action_debug_sync_tasks_titles" type="object" string="Enviar Títulos a Trello" class="btn-secondary"/>
                            <button name="action_debug_sync_tags" type="object" string="Enviar Etiquetas a Trello" class="btn-secondary"/>
                            <button name="action_debug_sync_descriptions" type="object" string="Enviar Descripciones a Trello" class="btn-secondary"/>
                            <button name="action_trello_sync_now" type="object" string="Enviar Todo a Trello" class="btn-primary"/>
                        </group>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
